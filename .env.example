# Copy this file to .env and adjust as needed.
# Postgres roda via `docker compose up db -d` (serviço `db` do docker-compose.yml),
# publicado em localhost:5432 pro Next.js local conseguir conectar direto.
DATABASE_URL="postgresql://animebattler:animebattler@localhost:5432/animebattler?schema=public"
